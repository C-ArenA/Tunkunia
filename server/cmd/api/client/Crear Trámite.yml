info:
  name: Crear Trámite
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/tramites
  body:
    type: json
    data: |-
      {
        "name": "Carlos"
      }
  auth:
    type: apikey
    key: X-TUNKUNIA-KEY
    value: "4354"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
