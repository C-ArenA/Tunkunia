info:
  name: health
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/health
  auth:
    type: apikey
    key: X-TUNKUNIA-KEY
    value: "4354"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
