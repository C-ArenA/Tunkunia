// This file is auto-generated by @hey-api/openapi-ts

import { type _JSONValue, defineQueryOptions, type UseMutationOptions } from '@pinia/colada';

import { serializeQueryKeyValue } from '../client';
import { client } from '../client.gen';
import { createTramite, deleteTramite, getCatalogHealth, getHealth, getTramite, listTramites, type Options, updateTramite } from '../sdk.gen';
import type { CreateTramiteData, CreateTramiteError, CreateTramiteResponse, DeleteTramiteData, DeleteTramiteError, DeleteTramiteResponse, GetCatalogHealthData, GetCatalogHealthError, GetCatalogHealthResponse, GetHealthData, GetHealthError, GetHealthResponse, GetTramiteData, GetTramiteError, GetTramiteResponse, ListTramitesData, ListTramitesError, ListTramitesResponse, UpdateTramiteData, UpdateTramiteError, UpdateTramiteResponse } from '../types.gen';

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'path'> & {
        _id: string;
        baseUrl?: _JSONValue;
        body?: _JSONValue;
        query?: _JSONValue;
        tags?: _JSONValue;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl } as QueryKey<TOptions>[0];
    if (tags) {
        params.tags = tags as unknown as _JSONValue;
    }
    if (options?.body !== undefined) {
        const normalizedBody = serializeQueryKeyValue(options.body);
        if (normalizedBody !== undefined) {
            params.body = normalizedBody;
        }
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query !== undefined) {
        const normalizedQuery = serializeQueryKeyValue(options.query);
        if (normalizedQuery !== undefined) {
            params.query = normalizedQuery;
        }
    }
    return [params];
};

export const getHealthQueryKey = (options?: Options<GetHealthData>) => createQueryKey('getHealth', options);

/**
 * Salud Módulo
 */
export const getHealthQuery = defineQueryOptions<Options<GetHealthData>, GetHealthResponse, GetHealthError>((options?: Options<GetHealthData>) => ({
    key: getHealthQueryKey(options),
    query: async (context) => {
        const { data } = await getHealth({
            ...options,
            ...context,
            throwOnError: true
        });
        return data;
    }
}));

export const getCatalogHealthQueryKey = (options?: Options<GetCatalogHealthData>) => createQueryKey('getCatalogHealth', options);

/**
 * Salud Módulo
 */
export const getCatalogHealthQuery = defineQueryOptions<Options<GetCatalogHealthData>, GetCatalogHealthResponse, GetCatalogHealthError>((options?: Options<GetCatalogHealthData>) => ({
    key: getCatalogHealthQueryKey(options),
    query: async (context) => {
        const { data } = await getCatalogHealth({
            ...options,
            ...context,
            throwOnError: true
        });
        return data;
    }
}));

export const listTramitesQueryKey = (options?: Options<ListTramitesData>) => createQueryKey('listTramites', options);

/**
 * Listar Trámites
 *
 * Obtiene una lista de trámites paginados
 * - **Guests** will only see `published` items.
 * - **Creators** will see their own drafts/archived items alongside published ones.
 * - **Admins** will see everything.
 *
 */
export const listTramitesQuery = defineQueryOptions<Options<ListTramitesData>, ListTramitesResponse, ListTramitesError>((options?: Options<ListTramitesData>) => ({
    key: listTramitesQueryKey(options),
    query: async (context) => {
        const { data } = await listTramites({
            ...options,
            ...context,
            throwOnError: true
        });
        return data;
    }
}));

/**
 * Crear Trámite
 *
 * Permite a usuarios con rol de administración la creación de nuevos trámites en el catálogo de trámites. Los mismos nacerán con estado draft y pueden contener un procedimiento vacío
 */
export const createTramiteMutation = (options?: Partial<Options<CreateTramiteData>>): UseMutationOptions<CreateTramiteResponse, Options<CreateTramiteData>, CreateTramiteError> => ({
    mutation: async (vars) => {
        const { data } = await createTramite({
            ...options,
            ...vars,
            throwOnError: true
        });
        return data;
    }
});

/**
 * Eliminar Trámite
 */
export const deleteTramiteMutation = (options?: Partial<Options<DeleteTramiteData>>): UseMutationOptions<DeleteTramiteResponse, Options<DeleteTramiteData>, DeleteTramiteError> => ({
    mutation: async (vars) => {
        const { data } = await deleteTramite({
            ...options,
            ...vars,
            throwOnError: true
        });
        return data;
    }
});

export const getTramiteQueryKey = (options: Options<GetTramiteData>) => createQueryKey('getTramite', options);

/**
 * Obtener Trámite
 */
export const getTramiteQuery = defineQueryOptions<Options<GetTramiteData>, GetTramiteResponse, GetTramiteError>((options: Options<GetTramiteData>) => ({
    key: getTramiteQueryKey(options),
    query: async (context) => {
        const { data } = await getTramite({
            ...options,
            ...context,
            throwOnError: true
        });
        return data;
    }
}));

/**
 * Modificar Trámite
 */
export const updateTramiteMutation = (options?: Partial<Options<UpdateTramiteData>>): UseMutationOptions<UpdateTramiteResponse, Options<UpdateTramiteData>, UpdateTramiteError> => ({
    mutation: async (vars) => {
        const { data } = await updateTramite({
            ...options,
            ...vars,
            throwOnError: true
        });
        return data;
    }
});
