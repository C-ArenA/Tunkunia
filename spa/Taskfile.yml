# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: "3"

output: prefixed

tasks:
  dev:
    prefix: NUXT
    desc: Run UI dev server
    cmds:
      - pnpm dev

  build:
    desc: Build UI for production
    cmds:
      - pnpm gen
